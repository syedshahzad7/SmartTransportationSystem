<routes>
    <!-- Vehicle type definition -->
    <vType id="car" accel="1.0" decel="2.0" sigma="0.5" length="5" maxSpeed="13.9"/>

    <!-- A vehicle that departs at time=0 and travels from junction 0 to 1 -->
    <route id="route0" edges="road0"/>
    <vehicle id="Car1" type="car" route="route0" depart="0"/>
</routes>